auto:        true
pygments:    true
markdown:    rdiscount
server:      true

redcarpet:
  extensions: [:filter_html, :autolink, :fenced_code, :hard_wrap, :tables, :gh_blockcode]
  
rdiscount:
  extensions: [:autolink]